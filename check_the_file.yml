--- 

  - hosts: all
    become: true
    tasks:

    - name: check the file
      stat: 
        path: /etc/hosts
      register: ww

    - name:  SHOW THE RESOULT
      debug:
        msg: File_attributes "{{ ww }}"
